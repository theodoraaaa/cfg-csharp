// Concatenation

string greeting = "Hello";
string userName = "CFG";

Console.WriteLine(greeting + ", " + userName + "!");

// Interpolation

Console.WriteLine($"{greeting}, {userName}!"); 

// everything inside "" is treated as text, apart from whats inside {} which is treat as code